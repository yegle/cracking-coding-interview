#include<iostream>

using namespace std;

void insert_at(char* base, char* insertion, int pos) {
}

void right_shift(char* str, int len, int pos, int distance, char padding) {
}

int main(int argc, char* argv[]) {
    if (argc != 3) {
        cout << "Usage: " << argv[0] << " string_to_be_replaced true_length" << endl;
        return 1;
    }
    return 0;
}
